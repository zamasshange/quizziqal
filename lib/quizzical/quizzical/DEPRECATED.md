# Deprecated — do not use

This folder is a **stale copy** of an older Quizzical app. The live project is the **repository root** (`c:\Users\Lenovo\Quizzical`).

- Deploy from the repo root, not this folder.
- Do not run `npm run dev` or `npm run build` here.
- All active development happens in `/app`, `/components`, `/lib` at the root.

Safe to delete this folder once you confirm nothing references it.
