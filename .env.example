# Optional — quizzes work without these (uses bootstrap + Wikipedia)

# Better AI-generated wrong answers
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-4o-mini

# Movie picture quizzes
TMDB_READ_TOKEN=

# Persistent quiz cache (falls back to .mock-data/ locally)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
